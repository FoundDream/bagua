# Vertex AI Express Mode API Key
# 创建方式（任选其一）：
#   1) https://aistudio.google.com/apikey  → 直接生成 (兼容 Vertex Express)
#   2) GCP Console → Vertex AI → "Get API key" (Express Mode)
VERTEX_API_KEY=your-vertex-api-key

# 模型名（默认 gemini-2.5-pro，可换 gemini-2.5-flash 等）
VERTEX_MODEL=gemini-2.5-pro
